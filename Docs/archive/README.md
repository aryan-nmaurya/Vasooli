# Archive

Pre-build planning documents, kept for the record.

**Neither describes the system as built.** Both specify Razorpay Smart Collect /
Virtual Accounts, which Razorpay confirmed is unavailable for this merchant's business
type — collection runs on Payment Links instead.

| Document | What it was |
|---|---|
| `Vasooli_Documentation.md` | The original product spec, written before any code |
| `Vasooli_Implementation_Plan.md` | The 14-phase build plan derived from it |

For what actually exists, read [`../../README.md`](../../README.md) — it is the single
canonical architecture description.
